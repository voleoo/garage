%p#notice= notice

.service= @service.service

.brands
  - @service.brands.each do |brand|
    .brand= brand.brand
.users
  - expert_users.each do |user|
    .user= user.first_name

= link_to 'Edit', edit_service_path(@service)
\|
= link_to 'Back', services_path
